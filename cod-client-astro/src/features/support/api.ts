import { apiFetch } from "@/lib/api";
type Envelope<T> = { success: true; data: T };
export type SupportChannel = { id: string; type: "whatsapp" | "email"; name: string; enabled: boolean; provider: string; senderId: string | null; accessTokenMasked: string; verifyTokenMasked: string; appSecretMasked: string; webhookSecretMasked: string; updatedAt: string; webhookUrl: string };
export type Conversation = { id: string; channelId: string; channelType: "whatsapp" | "email"; channelName: string; customerId: string | null; orderId: string | null; contact: string; contactName: string | null; subject: string | null; status: "open" | "pending" | "resolved" | "closed"; priority: "low" | "normal" | "high" | "urgent"; assigneeId: string | null; assigneeName: string | null; unreadCount: number; lastMessageAt: string };
export type SupportMessage = { id: string; direction: "inbound" | "outbound" | "internal"; channelType: "whatsapp" | "email"; senderName: string | null; body: string; deliveryStatus: string; errorCode: string | null; createdAt: string };
export type SupportTicket = { id: string; ticketNumber: string; conversationId: string | null; customerId: string | null; orderId: string | null; subject: string; description: string | null; status: "open" | "in_progress" | "waiting_customer" | "resolved" | "closed"; priority: "low" | "normal" | "high" | "urgent"; assigneeId: string | null; assigneeName: string | null; dueAt: string | null; createdAt: string; updatedAt: string };
export const listChannels = async () => (await apiFetch<Envelope<SupportChannel[]>>("/api/support/channels")).data;
export const saveChannel = async (type: SupportChannel["type"], data: Record<string, unknown>) => (await apiFetch<Envelope<SupportChannel>>(`/api/support/channels/${type}`, { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify(data) })).data;
export const listConversations = async () => (await apiFetch<Envelope<Conversation[]>>("/api/support/conversations")).data;
export const createConversation = async (data: Record<string, unknown>) => (await apiFetch<Envelope<Conversation>>("/api/support/conversations", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(data) })).data;
export const listMessages = async (id: string) => (await apiFetch<Envelope<SupportMessage[]>>(`/api/support/conversations/${id}/messages`)).data;
export const sendMessage = async (id: string, body: string, internal = false) => (await apiFetch<Envelope<SupportMessage>>(`/api/support/conversations/${id}/messages`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ body, internal }) })).data;
export const updateConversation = (id: string, data: Partial<Pick<Conversation, "status" | "priority" | "assigneeId">>) => apiFetch(`/api/support/conversations/${id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(data) });
export const listTickets = async () => (await apiFetch<Envelope<SupportTicket[]>>("/api/support/tickets")).data;
export const createTicket = async (data: Record<string, unknown>) => (await apiFetch<Envelope<SupportTicket>>("/api/support/tickets", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(data) })).data;
export const updateTicket = (id: string, data: Partial<Pick<SupportTicket, "status" | "priority" | "assigneeId" | "dueAt">>) => apiFetch(`/api/support/tickets/${id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(data) });
export const createCustomerLink = async (orderId: string) => (await apiFetch<Envelope<{ url: string; expiresAt: string }>>(`/api/support/orders/${orderId}/customer-link`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ expiresInDays: 30 }) })).data;
